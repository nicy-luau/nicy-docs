# Platform Matrix

## Target families

- Windows: x64, x86, arm64
- Linux: x64, x86, arm64
- macOS: x64, arm64
- Android/Termux: arm64, armv7

## Release validation checklist

1. CLI starts and prints runtime version
2. runtime symbols resolve
3. script execution succeeds
4. native module smoke test passes
